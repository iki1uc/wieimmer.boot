const nodes = [
  { id:'3', angle:-90 }, 
  { id:'9', angle:-45 },
  { id:'◎', angle:0 },
  { id:'81', angle:45 },
  { id:'◆', angle:90 },
  { id:'△', angle:135 },
  { id:'27', angle:180 },
  { id:'▣', angle:225 }
];
const path = ['3','9','◎','81','◆','△','27','▣','3'];
idx === Math.floor(Engine.state.pump/12) % nodes.length
const orbitLevel = (cell.orbit + cell.qi + cell.iqq) % 8;
const echo = (cell.ghost + cell.in + cell.out) % 8;
const echo = (cell.ghost + cell.in + cell.out) % 8;
const drift = ((Engine.state.pump + Engine.state.cpuJumpC) % 8) * 0.1;
const trace = ((Engine.state.pump + Engine.state.cpuJumpC) % 8) / 2;
const mystery = (qi + iqq + octa) % 8;
const moon = Math.floor((shadow * depthFactor) % 8);
